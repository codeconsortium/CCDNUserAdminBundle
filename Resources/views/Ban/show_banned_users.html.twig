{% extends param('ccdn_user_admin.ban.show_banned_users.layout_template') %}

{% block stylesheets %}
	{{ parent() }}
{% endblock %}

{% block title %}{{- truncDot('title.user.admin.banned_users' | trans({}, 'CCDNUserAdminBundle'), param('ccdn_user_admin.seo.title_length')) -}}{% endblock %}

{% block sidebar %}
	{{ parent() }}

	{%- include 'CCDNUserAdminBundle:Sidebar:sidebar.html.twig' -%}

{% endblock %}


{% block body %}

	{%- spaceless -%}

	{%- include 'CCDNComponentCommonBundle:Flashes:flashes.html.twig' -%}

	{%- include 'CCDNComponentCrumbTrailBundle:Trail:trail.html.twig' with {'crumbs': crumbs } -%}

	<h2>{{- 'title.user.admin.banned_users' | trans({}, 'CCDNUserAdminBundle') -}}</h2>

	<div class="btn-toolbar" style="display:block !important;clear:both !important;">
		<div class="pull-left">
		</div>
		{%- if pager.haveToPaginate -%}
			<div class="pull-right">
		    	{{- pagerfanta(pager, 'twitter_bootstrap_compact', {'routeName': 'cc_admin_user_show_banned_paginated' }) -}}
			</div>
		{%- endif -%}
		<br><br>
	</div>

	<table class="table">
		<thead>
			<tr>
				<th class="left">{{- 'member.table.head.username' | trans({}, 'CCDNUserMemberBundle') -}}</th>
				<th class="left">{{- 'user.table.head.email' | trans({}, 'CCDNUserAdminBundle') -}}</th>
				<th class="center">{{- 'member.table.head.joined_date' | trans({}, 'CCDNUserMemberBundle') -}}</th>
				<th class="center">{{- 'user.table.head.status.activation' | trans({}, 'CCDNUserAdminBundle') -}}</th>
			</tr>
		</thead>
		<tbody>
		{%- for row, user in users -%}
			<tr class="{{- cycle(['row_odd', 'row_even'], row) }} marked_read">
				<td class="left"><a href="{{- path('cc_admin_user_show', {'user_id': user.getId }) -}}">{{- user.username |capitalize -}}</a></td>
				<td class="left">{{- user.email -}}</td>
				<td class="center">{{- user.registeredDate |date(param('ccdn_user_admin.ban.show_banned_users.member_since_datetime_format')) -}}</td>
				<td class="center">{%- if user.enabled -%}Activated{%- else -%}Pending Activation{%- endif -%}</td>
			</tr>
		{%- else -%}
			<tr>
				<td class="center" colspan="4">
					{{- 'users.banned.table.empty' | trans({}, 'CCDNUserAdminBundle') -}}
				</td>
			</tr>
		{%- endfor -%}
		</tbody>
	</table>

	<div class="btn-toolbar" style="display:block !important;clear:both !important;">
		<div class="pull-left">
		</div>
		{%- if pager.haveToPaginate -%}
			<div class="pull-right">
		    	{{- pagerfanta(pager, 'twitter_bootstrap_compact', {'routeName': 'cc_admin_user_show_banned_paginated' }) -}}
			</div>
		{%- endif -%}
		<br><br>
	</div>

	{%- include 'CCDNComponentCrumbTrailBundle:Trail:trail.html.twig' with {'crumbs': crumbs } -%}

	{%- endspaceless -%}

{% endblock %}
