{% extends param('ccdn_user_admin.role.set_users_role.layout_template') %}

{% block stylesheets %}
	{{ parent() }}
{% endblock %}

{% block title %}{{- truncDot('title.account.edit' |trans({}, 'CCDNUserUserBundle'), param('ccdn_user_admin.seo.title_length')) -}}{% endblock %}

{% block sidebar %}
	{{ parent() }}

	{%- include 'CCDNUserAdminBundle:Sidebar:sidebar.html.twig' -%}

{% endblock %}


{% block body %}

	{%- spaceless -%}

	{%- include 'CCDNComponentCommonBundle:Flashes:flashes.html.twig' -%}

	<h2>{{- 'title.account.edit' |trans({}, 'CCDNUserUserBundle') -}}</h2>

	{%- form_theme form param('ccdn_user_admin.role.set_users_role.form_theme') -%}
	<form action="{{- path('ccdn_user_admin_set_roles', {'userId': user.id }) -}}" {{ form_enctype(form) }} method="POST" class="form-horizontal">
		<fieldset>

			{{- form_errors(form) -}}

			<div class="control-group"><div class="controls row-fluid">{{- form_row(form._token) -}}</div></div>

			<div class="control-group">
				{{- form_label(form.role, null, { 'label': 'ccdn_user_admin.form.label.user.roles' |trans({}, 'CCDNUserAdminBundle')}) -}}</td>
				<div class="controls row-fluid">
					{{- form_errors(form.role) -}}
					{{- form_widget(form.role) -}}
				</div>
			</div>

			<div class="form-actions">
		        <input type="submit" class="btn btn-danger" value="{{- 'profile.edit.submit'|trans({}, 'FOSUserBundle') -}}">
			</div>

		</fieldset>
	</form>

	{%- endspaceless -%}

{% endblock body %}
